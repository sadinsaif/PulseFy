# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
Thumbs.db
*.log
*.pem

# env files
.env
.env*.local
.env.local

# vercel
.vercel

# drizzle
/drizzle

# Solana keypairs / private keys — NEVER commit these.
# Token-mint authority + treasury keys live here; leaking them = losing the funds.
/.keys/
*.keypair.json
id.json

# local tooling — Claude session/plan data and an unrelated desktop app's SQLite DB
/.claude/
/.freebuff/
